BoxLayout:
    orientation: "vertical"
    BoxLayout:
        id: button_box
    BoxLayout:
        Label:
            text: str(app.total_price)  # Convert to string for display
        Button:
            text: "reset"
            background_color: (0, 1, 1, 1)
            on_release: app.handle_reset()
